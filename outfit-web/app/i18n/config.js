export const i18nConfig = {
  // 支持的语言列表
  supportedLangs: ['en', 'zh-CN'],
  
  // 默认语言
  defaultLang: 'en',
  
  // 命名空间 - 按功能区域划分文案
  namespaces: [
    'common',    // 通用UI元素、按钮、导航等
    'apparel',   // 衣物类别、款式、属性等
    'matching'   // 搭配结果、规则描述等
  ],
  
  // 默认命名空间
  defaultNamespace: 'common'
}; 